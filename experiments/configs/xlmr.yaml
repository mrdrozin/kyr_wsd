# XLM-RoBERTa base — сильный мультиязычный бейзлайн.
# Крупная модель (270M), чувствительна к learning rate — ниже базового;
# меньший batch из-за объёма памяти даже на A100 (270M + до 11 глоссов на инстанс).
model_name: xlm-roberta-base
lr: 1.0e-5
batch_size: 16
